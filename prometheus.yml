global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: chatphp
    static_configs:
      - targets:
          - chatphp.erwintimo.courses.studalya.net